{
  "name": "zoo-management-system",
  "version": "1.0.0",
  "description": "Zoo Management System project root",
  "private": true,
  "scripts": {
    "dev:frontend": "cd admin-panel && npm run dev",
    "dev:backend": "cd Backend && npm run dev",
    "install:all": "npm install && cd admin-panel && npm install && cd ../Backend && npm install"
  }
}